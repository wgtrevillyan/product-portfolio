{
  "name": "product-portfolio",
  "version": "1.0.0",
  "description": "William Trevillyan's Product Portfolio",
  "scripts": {
    "build:footer": "node scripts/inject-footer.js",
    "build:header": "node scripts/inject-header.js",
    "build": "npm run build:header && npm run build:footer",
    "cleanup": "node scripts/cleanup-placeholders.js"
  },
  "devDependencies": {},
  "dependencies": {
    "nodemailer": "^6.9.16"
  }
}
